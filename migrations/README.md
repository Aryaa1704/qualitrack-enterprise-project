# Migrations

This folder is reserved for Alembic-ready database migrations.

Phase 0 intentionally creates no database models and no database tables. Future phases can add Alembic configuration and migration revisions here when models are introduced.
